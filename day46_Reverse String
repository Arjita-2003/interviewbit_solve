public class Solution {
    public String reverseString(String A) {
        int n = A.length();
        String ans = "";
        for(int i=n-1;i>=0;i--){
            char ch = A.charAt(i);
            ans += ch;
        }
        return ans;
    }
}
